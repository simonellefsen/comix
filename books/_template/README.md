Copy this folder to `books/<book-id>/<story-id>/`.

Required:

- `index.html` — the strip (speech under the art; place, time, and sound on the picture)
- `panels/` — one image per panel, plus `00-cover.jpg` if you have a cover

Then add the story to `books.json` and to the book’s `index.html`.
